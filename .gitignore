node_modules/
dist/
*.log

# Build/runtime artifacts
test/shots/

# Original game files + extracted assets are the user's own copy of the game.
# They are NOT redistributed in the repo — regenerate locally with
#   bash tools/stage_assets.sh "path/to/SnailMail.dat"
exe_src/
extracted/
public/assets/
public/assets-hd/
assets-hd/
# downloaded game zip + its extraction (install.sh)
.game/
# local Python venv for the HD upscaler (install.sh)
.venv/
# local Python CUDA venv for Real-ESRGAN super-resolution (tools/sr_upres.py)
.venv-sr/
__pycache__/
